delete observer
swap gt and observer
detect and accept observer rtstruct as dicom input
log rejected observer inputs
backup of state after relevant operations
create multiple observer files at once (import CSV, use first column)
keep database of other columns
export other columns when collating data
split teachertool in tabs
backup options: all images and all state for selected study
make temporary distance transform function using luamatch
 (in structure set url, 3 names, out png image per slice)
